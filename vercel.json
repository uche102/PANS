{
  "builds": [
    {
      "src": "election-site/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "^/admin(?:/.*)?$", "dest": "/admin/index.html" },
    { "src": "^/(.*)$", "dest": "/index.html" }
  ]
}
